# Phase 0 uses disposable local-only values. Do not reuse these in production.
POSTGRES_DB=axit
POSTGRES_USER=axit
POSTGRES_PASSWORD=axit-phase0-only
PUBLIC_ORIGIN=http://localhost:3000
PUBLIC_HOST=localhost:3000
SESSION_COOKIE_SECURE=false
