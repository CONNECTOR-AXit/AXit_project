{
  "name": "@axit/g0-document-viewer-proof",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.17.0 <25"
  },
  "scripts": {
    "fixtures": "uv run python fixtures/capture_real_payloads.py && node fixtures/build-fixtures.mjs --check",
    "fixtures:check": "node fixtures/build-fixtures.mjs --check",
    "serve": "node server.mjs",
    "pretest": "node tests/prepare-browser-evidence.mjs",
    "test": "node fixtures/build-fixtures.mjs --check && node --test tests/attestation.node.mjs && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.61.0"
  }
}
